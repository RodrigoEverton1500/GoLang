package main
import ("fmt"
"errors")

func f(slice[]int, func()) (erro error) {
    n, c := 0
    s := [] int {}
    s
    
    for {
        fmt.Scan(&n)
        if n == 0 {
            break
        }
        
        s[i] = n
        c++
    }
    
    for i := 0; i < c; i++ {
        
    }
}
